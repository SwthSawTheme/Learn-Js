

let array = [1,2,3,4,5,6,7,8]

const double = array.map((n) => n * 2)

console.log(double)

var people = [
    {name: "clary",age:32},
    {name: "Saw", age:22},
    {name: "Mex", age:54}
]

const peopleNomarlaized = people.map((n) => n.name.toLocaleUpperCase())

console.log(peopleNomarlaized)